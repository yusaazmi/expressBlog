"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var query_generator_internal_exports = {};
__export(query_generator_internal_exports, {
  MsSqlQueryGeneratorInternal: () => MsSqlQueryGeneratorInternal
});
module.exports = __toCommonJS(query_generator_internal_exports);
var import_query_generator_internal = require("../abstract/query-generator-internal.js");
const TECHNICAL_DATABASE_NAMES = Object.freeze(["master", "model", "msdb", "tempdb"]);
const TECHNICAL_SCHEMA_NAMES = Object.freeze([
  "db_accessadmin",
  "db_backupoperator",
  "db_datareader",
  "db_datawriter",
  "db_ddladmin",
  "db_denydatareader",
  "db_denydatawriter",
  "db_owner",
  "db_securityadmin",
  "INFORMATION_SCHEMA",
  "sys"
]);
class MsSqlQueryGeneratorInternal extends import_query_generator_internal.AbstractQueryGeneratorInternal {
  getTechnicalDatabaseNames() {
    return TECHNICAL_DATABASE_NAMES;
  }
  getTechnicalSchemaNames() {
    return TECHNICAL_SCHEMA_NAMES;
  }
  addLimitAndOffset(options) {
    let fragment = "";
    if (options.offset || options.limit) {
      fragment += ` OFFSET ${this.queryGenerator.escape(options.offset || 0, options)} ROWS`;
    }
    if (options.limit != null) {
      if (options.limit === 0) {
        throw new Error(`LIMIT 0 is not supported by ${this.dialect.name} dialect.`);
      }
      fragment += ` FETCH NEXT ${this.queryGenerator.escape(options.limit, options)} ROWS ONLY`;
    }
    return fragment;
  }
}
//# sourceMappingURL=query-generator-internal.js.map

import type { QueryRawOptions } from '../../sequelize';
import type { TableNameOrModel } from '../abstract/query-generator-typescript';
import { AbstractQueryInterfaceInternal } from '../abstract/query-interface-internal';
import type { SqliteColumnsDescription } from './query-interface.types';
import type { SqliteDialect } from './index.js';
export declare class SqliteQueryInterfaceInternal extends AbstractQueryInterfaceInternal {
    #private;
    readonly dialect: SqliteDialect;
    constructor(dialect: SqliteDialect);
    /**
     * Alters a table in sqlite.
     * Workaround for sqlite's limited alter table support.
     *
     * @param tableName
     * @param columns
     * @param options
     */
    alterTableInternal(tableName: TableNameOrModel, columns: SqliteColumnsDescription, options?: QueryRawOptions): Promise<void>;
}
